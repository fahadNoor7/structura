vaadin.frontend.hotdeploy=true

# DEBUG is enabled only under the 'dev' profile — never in the base config,
# which is loaded by all environments (policy: environments default to INFO).
logging.level.com.wsd.structura=DEBUG
